<% provide(:title, @user.name) %>
<h1>
  <%= gravatar_for @user %>
  <%= @user.name %>

  <% if @user == current_user %>
  	<%= link_to "Settings", edit_user_path(current_user) %>
  <% end %>
  
</h1>